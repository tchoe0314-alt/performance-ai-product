[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.imagery-gateway"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
